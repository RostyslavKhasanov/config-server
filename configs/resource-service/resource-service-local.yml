spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/resource-db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
  application:
    name: resource-service
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8080

localstack:
  s3:
    url: http://localhost:4566
    bucket-name: resource-bucket
    region: us-east-1
    accessKey: test
    secretKey: test